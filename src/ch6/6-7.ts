let height: number = 10; // 실제 값은 해당 변수가 어떻게 사용되는지에 따라 변경해야 합니다.
let width: number = 10; // 실제 값은 해당 변수가 어떻게 사용되는지에 따라 변경해야 합니다.
let a: number = height * width;

const cpyNm: string = '애플';

let tpHd: string = '제목없음';
let result: string = `<h1>${tpHd}</h1>`;